/**
 * Class for mock related functions
 * 1. Create mock
 * 2. Update mock
 *
 * @param {Object} config - parsed config
 */
function Mock(config) {
  this.mock = config.mock;
}

module.exports = Mock;
